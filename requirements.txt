# HTTP requests for potential future data ingestion
requests==2.31.0

# Local LLM orchestration (The core of our agents)
ollama==0.1.6

# Environment variable management (For securing the .env file)
python-dotenv==1.0.0

